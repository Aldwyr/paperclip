# Paperclip Runner six-hour stress campaign

Started: 2026-08-23 05:48 America/Chicago

This is the durable evidence ledger for the provider-neutral Paperclip Runner
stress campaign. It records only non-secret configuration and links to the
authoritative local issues/runs/traces.

## Baseline

- Instance: `http://localhost:3100`, company `DOT`
- Branch: `PAP-16679-paperclip-runner` at `a70b0f70` with pre-existing local changes
- Server: `2026.722.0+816.git.a70b0f70.dirty`
- Running provider agents:
  - Runner: `paperclip_runner`, Codex, `gpt-5.6-luna`, raw tracing enabled
  - OpenCode Runner: `paperclip_runner`, OpenCode, `opencode/big-pickle`, raw tracing enabled
- Environment probes:
  - Codex `gpt-5.6-luna`: pass
  - OpenCode `opencode/big-pickle`: pass
  - ACPX Claude `claude-sonnet-5`: pass after binding the existing encrypted company Anthropic secret; authenticated live turn completed
  - ACPX Codex `gpt-5.6-sol`: pass
  - ACPX Pi `openrouter/deepseek/deepseek-v4-flash-0731`: pass after importing `OPENROUTER_API_KEY` into the encrypted company secret store and binding it by reference
- Model substitutions:
  - No substitution is currently required: the live OpenRouter catalog qualified Kimi Code, GLM, GPT-OSS, and GLM Flash configurations.
  - Secret values are never persisted in agent configuration, this ledger, trace metadata, or command output.
- Canonical PRP action roster: 41 actions; 14 production bindings; remaining actions use safe isolated conformance fixtures where live execution is not authoritative or would be destructive.

## Live execution ledger

| Time | Provider/config | Scenario | Issue | Run | Expected | Actual | Trace | Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 11:20Z | ACPX Claude / `claude-sonnet-5` | Ask-mode long answer (pre-fix control) | [DOT-102](http://localhost:3100/DOT/issues/DOT-102) | `faddb6ec-c80b-4297-a098-c400d8d30ff8` | Immediate commentary, long final, done | Rejected before provider: native runtime excluded ask mode; two bounded continuations, then blocked | Incomplete, 0 frames | FAIL |
| 11:20Z | OpenCode / `nemotron-3.5-lightning-free` | Ask-mode concise answer (pre-fix control) | [DOT-104](http://localhost:3100/DOT/issues/DOT-104) | `fd4e5a65-2646-46ed-87bf-4fcf0d63fb73` | Immediate commentary, concise final, done | Rejected before provider by the same ask-mode gate; bounded, not infinite | Incomplete, 0 frames | FAIL |
| 11:20Z | ACPX Codex / `gpt-5.6-sol` | Exact 11-byte file (pre-fix control) | [DOT-103](http://localhost:3100/DOT/issues/DOT-103) | `eb7fcbb1-f527-458d-bd0c-ae6ec30712d2` | File/tool stream, byte/hash evidence, done | Launch omitted ACPX runtime directory; recovery then failed closed on missing checkpoint | Capturing metadata, 0 frames | FAIL |
| 11:20Z | OpenCode / `nemotron-3-ultra-free` | Dependency-free module plus `node:test` | [DOT-105](http://localhost:3100/DOT/issues/DOT-105) | `20f193a6-6bab-43a9-a8cc-69f4333d5f67` | Commentary/reasoning/tool/file/test stream, exact final, done | 107 durable events; commands and writes visible; 8/8 tests; one final assistant comment; issue done | Complete, 507 frames / 328,229 bytes | PASS |
| 11:24Z | OpenCode / `nemotron-3.5-lightning-free` | Ask-mode concise answer (post-fix) | [DOT-106](http://localhost:3100/DOT/issues/DOT-106) | `c2173057-6243-484f-9fce-c8c6ad192913` | Direct answer, no file mutation, done | Admitted with a v2 ask envelope; provider turn in progress | Capturing | RUNNING |
| 11:24Z | ACPX Claude / `claude-sonnet-5` | Ask-mode long answer (post-fix) | [DOT-107](http://localhost:3100/DOT/issues/DOT-107) | `f487c4c0-7923-43bf-8468-4ce48015d06d` | Direct long answer | Ask envelope admitted, then provider emitted actionable `Authentication required` notice and `turn.failed`; bounded retry scheduled | Capturing, 0 frames | EXPECTED ENV FAILURE |
| 11:24Z | ACPX Codex / `gpt-5.6-sol` | Exact 11-byte file (post-fix) | [DOT-108](http://localhost:3100/DOT/issues/DOT-108) | `6ec04087-9c29-47ce-b038-7cb2598c6668` | Commentary/tool stream, byte/hash evidence, done | ACPX launched; streamed agent text/thinking and shell execution; verified 11 bytes and SHA-256 `e49c81…d78ee`; finalization pending | Capturing | RUNNING |
| 11:31Z | ACPX Pi / OpenRouter DeepSeek Flash | Exact JSON file plus runtime approval | [DOT-109](http://localhost:3100/DOT/issues/DOT-109) | `8bbba551-6b1a-4603-bc61-a098e6dc9b5e` | Immediate commentary, approval, exact file/evidence, done | Immediate agent message and rich tool status; write outside the bound workspace was correctly denied; two provider approval requests expired/cancelled before the API accepted a response; provider continued with task context | Capturing | RUNNING / APPROVAL RACE |
| 11:31Z | ACPX Claude / `claude-sonnet-5` | Authenticated ask-mode response | [DOT-110](http://localhost:3100/DOT/issues/DOT-110) | `22ddf545-50fa-460a-924d-bd896b5fb4e3` | Immediate streamed answer, semantic result, done | 48 events; authenticated turn streamed the requested answer, emitted accepted semantic result, materialized one response, issue done | Captured | PASS |
| 11:31Z | OpenCode / OpenRouter Kimi Code | Multi-file slug library and tests | [DOT-111](http://localhost:3100/DOT/issues/DOT-111) | `8ec8f8ac-1420-44a4-aa86-f8722ae93b9a` | Reasoning/tools/files/tests/final, done | 70 events; model identity correct, implementation and verification completed, accepted semantic result, issue done | Captured | PASS |
| 11:31Z | OpenCode / OpenRouter GLM | Planning workflow | [DOT-112](http://localhost:3100/DOT/issues/DOT-112) | `bb5d2541-a0e4-4250-af97-e9592071d5e6` | Canonical plan, human confirmation, no implementation before approval | 81 events; browser showed the canonical plan card and Plan tab. Confirm created a narrow continuation, which delegated implementation to DOT-115; parent became legitimately dependency-blocked instead of implementing prematurely | Captured | PASS |
| 11:31Z | OpenCode / OpenRouter GPT-OSS 20B | Ask-mode completion-tool robustness | [DOT-113](http://localhost:3100/DOT/issues/DOT-113) | `df8c4715-69e6-405c-b116-93f903a78afd` | Concise answer and valid semantic finish | Exact trace proved the cheap model used schema alias `paperclip_paperclip_finish`, disposition `completed`, and criterion `passed`. Normalization compatibility was added. A forced server restart then exposed a stale-active terminal checkpoint in recovery | Capturing | FIXED / LIVE RETEST PENDING |
| 11:31Z | OpenCode / OpenRouter GLM Flash | File task with intentionally unavailable verifier | [DOT-114](http://localhost:3100/DOT/issues/DOT-114) | `a5b9b353-ac6d-4d03-a355-2a9c724223c9` | `not_run/tool_unavailable` caveat, accepted low-risk completion, done | File work and semantic result completed, but the UI selected an opening progress sentence as the durable final reply because OpenCode labels every text part identically | Captured | FAIL / PRESENTATION |
| 11:37Z | OpenCode / OpenRouter GLM | Accepted-plan implementation subtask | [DOT-115](http://localhost:3100/DOT/issues/DOT-115) | `8c01a6f6-b4a1-474b-9d4c-e05118456fc1` | Implement the approved plan, test, return result to parent | Created the CSV parser and tests; 15/15 pass. Survived forced server restart and process-loss recovery, settled done, and retained the parent dependency lineage | Captured | PASS |
| 11:40Z | OpenCode / OpenRouter GLM Flash | Post-fix unavailable-verifier regression | [DOT-116](http://localhost:3100/DOT/issues/DOT-116) | `dd09ed68-de34-460c-9720-fd4f5c664aba` | Opening commentary during work, exact post-work result as final, accepted `not_run/tool_unavailable` caveat | Issue done; presentation selected the substantive post-work response, not opening commentary; caveat accepted without a retry | Captured | PASS |
| 12:37Z | OpenCode / OpenRouter GPT-OSS 20B | Alias normalization and recovered-session completion | [DOT-113](http://localhost:3100/DOT/issues/DOT-113) | `6edfa131-af84-4f13-a06d-71cc201223e3` | Accept known provider aliases, reuse the provider session, complete once | Issue done after the recovered same-session turn; normalized semantic result accepted with criterion satisfied and canonical verification | Complete, 1,736 frames / 514,978 bytes, digest verified | PASS |
| 12:40Z | OpenCode / OpenRouter GPT-OSS 20B | Invalid completion behavior and bounded exhaustion | [DOT-117](http://localhost:3100/DOT/issues/DOT-117) | `29a9b534-92b7-4edf-8024-307bfba5b449` | Prose cannot impersonate semantic authority; retries bounded; failure visibly owned | Model printed completion-shaped JSON as ordinary final text on all three same-run attempts. Paperclip correctly rejected it, preserved the trace, stopped after attempt 3, and did not mark work done. Live control exposed stale cross-run watchdog counts and a non-actionable agent-owned terminal wait | Captured | EXPECTED PROVIDER FAILURE / RECOVERY FIXED, RETEST PENDING |
| 12:56Z | OpenCode / OpenRouter GPT-OSS 20B | Post-fix result-less exhaustion ownership | [DOT-121](http://localhost:3100/DOT/issues/DOT-121) | `44f1e71b-b0cd-424a-afdc-9b95bca65cd3` | Three same-run attempts, named terminal failure, board-owned recovery, no extra provider run | Weak model again printed rather than invoked the result. Run ended `failed/native_session_retry_exhausted`; issue is `in_review`; active action is board-owned with return owner, attempt 1/3, and no wake. No blind retry followed | Complete, 1,480 frames / 405,060 bytes | PASS (EXPECTED PROVIDER FAILURE) |
| 12:56Z | Codex app-server / `gpt-5.6-luna` | Structured ask-user, human answer, same-task continuation, exact bytes | [DOT-122](http://localhost:3100/DOT/issues/DOT-122) | `5703966e-b516-4dc6-bdd0-f480b8cb2be1` → `af84fd0a-7e70-4dfc-afdc-0095efe863c5` | Ask before work; visible owned wait; answer Beta; resume and write exactly `Beta\n`; one settled response | Browser verified immediate commentary, question card, Beta answer, continuation commentary/tools, exact hex `426574610a`, and one primary final reply in chronological order. Issue done | Complete, 52 + 159 frames; correlated completion group has 4 frames → 15 stages → 6 PRP events | PASS |
| 12:57Z | OpenCode / OpenRouter Kimi Code | Queued steering after a nearly settled turn | [DOT-123](http://localhost:3100/DOT/issues/DOT-123) | `3f73546b-87e9-4704-b3f2-ddcdfb57745a` → `bc6711e9-4c59-4f58-a00c-8a962a5d3b53` | Preserve first result, place queued message at consumption point, narrow continuation, no redo | Browser showed original snake_case result, then queued board message, then a same-provider-session continuation changing only the requested format. Kimi reused files, updated code/tests/docs, and finished with 25/25 tests | Complete, 1,778 + 934 frames | PASS WITH PRESENTATION DEFECT |
| 12:59Z | OpenCode / OpenRouter GLM Flash | Agent debug tracing disabled for one run | [DOT-124](http://localhost:3100/DOT/issues/DOT-124) | `afb4af9e-6def-4f3a-b7f3-f82263469dc5` | Run succeeds while raw store remains empty; PRP/presentation inspector remains available; setting restored | Issue done; runner profile snapshot contains no provider trace; trace API returns null/0; browser Inspector says capture was off and offers re-run with trace while showing canonical PRP/presentation | Off (0 frames); agent tracing restored immediately after run snapshot | PASS |
| 13:01Z | OpenCode / OpenRouter Kimi Code | Substantive final followed by missing-semantic-result recovery | [DOT-125](http://localhost:3100/DOT/issues/DOT-125) | `3b1e8460-8c55-4ffb-bc3f-13fecfbdee69` | Preserve the real final while obtaining disposition only; no duplicate implementation | Same native session recovered, but presentation selected the recovery acknowledgement instead of the pre-recovery substantive final | Complete, 860 frames / 368,508 bytes | FAIL / FIXED |
| 13:06Z | OpenCode / OpenRouter Kimi Code | Fresh post-fix multi-file implementation | [DOT-126](http://localhost:3100/DOT/issues/DOT-126) | `b59ac6ba-280e-4eec-94d3-1c0ab969a343` | Normal completion without semantic recovery; substantive final; done | 13/13 tests, one accepted result, substantive response, issue done, no retry loop | Captured | PASS |
| 13:12Z | Codex app-server / `gpt-5.6-luna` | True same-turn steering plus idempotent replay | [DOT-127](http://localhost:3100/DOT/issues/DOT-127) | `b8f9448c-2007-4c48-b388-be640903014b` | Inject steering into the active turn, acknowledge once, do not restart, preserve causal UI order | Steering accepted in 200ms; replay was idempotent; agent explicitly incorporated the requirement without restart; 55/55 tests. Browser now splits the settled activity at the durable acknowledgement: pre-steer Worked → human steering bubble → post-steer Worked → final reply | Complete; acknowledgement maps to durable PRP and activity lineage | PASS |
| 13:17Z | Codex app-server / `gpt-5.6-luna` | Native web search and correlated presentation | [DOT-128](http://localhost:3100/DOT/issues/DOT-128) | `e3b29f15-96c2-4ece-b266-0a5ed391d430` | Immediate commentary, rich search status, cited final, done | Browser showed immediate commentary, `Searched once`, concise reasoning, and a cited final. Inspector correlated raw `webSearch` frames 50–51 through 10 mapping stages into four PRP events and the visible research component | 360 frames / 176.9 KiB | PASS |
| 13:20Z | OpenCode / OpenRouter Kimi Code | Real failed-tool recovery and test completion | [DOT-129](http://localhost:3100/DOT/issues/DOT-129) | `98678b97-e35d-4e3f-8474-6133c0f2f66e` | Surface the failure, recover without looping, verify, done | Provider recovered from the genuine failed tool, completed implementation, and passed 9/9 tests; issue settled with a substantive final and artifact | Captured | PASS |
| 13:25Z | OpenCode / OpenRouter GLM | Human-only completion review | [DOT-130](http://localhost:3100/DOT/issues/DOT-130) | `b9a48b05-23c6-420a-bdda-5a5977c82f0e` | External verification becomes a named human review, never a blind provider retry | Issue entered `in_review`; browser card shows `Assigned to You · board only`, evidence, and durable review interaction `9975a62b…` | Captured | PASS / REJECT CONTINUATION PENDING |
| 13:30Z | ACPX Pi / OpenRouter DeepSeek Flash | Result-less native exhaustion and cross-run recovery | [DOT-131](http://localhost:3100/DOT/issues/DOT-131) | `b696206c-8ab5-47ef-a7da-a47e713de015` | Three bounded attempts, board recovery, no fourth run | Terminal `native_session_retry_exhausted` at attempt 3; issue `in_review`, active recovery board-owned, and no generic stranded continuation raced it | Captured | PASS (EXPECTED PROVIDER FAILURE) |
| 13:34Z | ACPX Pi / OpenRouter DeepSeek Flash | Operator cancellation presentation | [DOT-132](http://localhost:3100/DOT/issues/DOT-132) | two cancelled runs | Cancel active work; no stuck-running UI artifacts | Browser showed compact `Stopped` terminal state; unfinished provider/tool activity normalized to Interrupted rather than Running | Captured | PASS |
| 14:16Z | ACPX Pi / OpenRouter DeepSeek Flash | Structured question followed by response-wake continuation | [DOT-136](http://localhost:3100/DOT/issues/DOT-136) | `268e7c5d-860b-4188-b749-12f514fc996e` → `4391cce2-b87f-46e7-b643-07554ca708bd` | Park immediately, resume on answer, runtime approval, exact file, done | Initial governed wait parked in 11s with no polling. Replacement ACPX session consumed Maple, but its command approval expired after 30s; model then retried denied writes and became idle. Operator cancelled the hung run | Captured | FAIL / FIXED IN FOLLOW-UP |
| 14:20Z | ACPX Pi / OpenRouter DeepSeek Flash | Direct native runtime approval route | [DOT-138](http://localhost:3100/DOT/issues/DOT-138) | `eaf5f1b8-903f-41e8-8bea-0083fc51fd91` | Resolve an in-process ACPX request and resume its provider turn | Resolution accepted in 115ms and emitted `runtime_request.resolved`; browser UI also successfully resolved a later live request. A relative nested path was incorrectly classified as absolute/outside workspace, exposing a Pi extension bug | Captured | ROUTING PASS / PATH POLICY FAIL |
| 14:24Z | ACPX Pi / OpenRouter DeepSeek Flash | Session permission, nested write, exact-byte finish | [DOT-139](http://localhost:3100/DOT/issues/DOT-139) | `f142fe16-be67-4ecf-9766-1b5628138ea4` | One approval grants the session, relative nested write succeeds, exact 9 bytes, one finish | Exactly one request was created/resolved; no second prompt; three tools ran; file is `61 70 70 72 6f 76 65 64 0a`; semantic result accepted and issue done in 15s | Captured | PASS |
| 14:25Z | OpenCode / OpenRouter GLM | Human completion-review rejection and narrow repair | [DOT-130](http://localhost:3100/DOT/issues/DOT-130) | `da584ad0-86f3-4c60-979a-7fbfc279482f` | Reviewer rejects with one verification request; agent verifies only, returns to review, then approval completes | Continuation read the rejection, used a read-only Node check without rewriting work, created a replacement human-only review, and settled done after approval | Captured | PASS |
| 14:28Z | OpenCode / OpenRouter GLM Flash | Native review materialization while the issue page remains open | [DOT-140](http://localhost:3100/DOT/issues/DOT-140) | `b02caacf-c642-4daf-80ed-ece5580b8bfe` → `3ec2d616-700f-4330-9288-fb9f9cb38fb5` | Review card appears live without reload; board approves; continuation finishes | Browser received the native interaction invalidation, rendered the named human-only card immediately, and approval completed the issue | Captured | PASS |
| 14:34Z | OpenCode / OpenRouter GLM | Plan reject → revise → accept → delegate → child return | [DOT-142](http://localhost:3100/DOT/issues/DOT-142) → [DOT-143](http://localhost:3100/DOT/issues/DOT-143) | `359270cb…` → `b1dbdaa2…` → `aab9a6ff…` → `0f41f4b1…` → `13c87242…` | Rejection wakes a revision-only turn; acceptance creates one child; parent resumes once and completes | Exact rejection note appeared causally; revision 2 incorporated all changes without implementation; acceptance created one child; child passed 52 tests; parent returned and completed. The run also exposed annotated evidence parsing and stale-finalizer races, both fixed below | Captured | PASS AFTER FIXES |
| 14:45Z | Kimi child → GLM parent | Parent/dependency wake coalescing and server-restart recovery | [DOT-144](http://localhost:3100/DOT/issues/DOT-144) → [DOT-145](http://localhost:3100/DOT/issues/DOT-145) | `4f15ab70…` → `b858bf14…` / `0b365fe7…` | One rich parent wake, no duplicate provider run, child summary survives restart | Child completed 7 tests. Exactly one parent wake intent used `issue_children_completed`; forced restart interrupted and recovered the parent. Pre-projection control carried a null summary and motivated the semantic-result fallback | Captured | PASS / CONTROL |
| 14:49Z | Kimi child → GLM parent | Semantic child summary at status-commit time | [DOT-146](http://localhost:3100/DOT/issues/DOT-146) → [DOT-147](http://localhost:3100/DOT/issues/DOT-147) | `3863e7fd…` → `19bd4f55…` | Parent wake contains accepted child summary before comment materialization | Wake snapshot contained the full accepted semantic summary and only one parent run. The provider still performed broad discovery because native execution input omitted the wake delta, exposing STRESS-028 | Captured | LINEAGE PASS / PROMPT FAIL |
| 14:53Z | Kimi ask child → GLM parent | Closed native prompt carries child handoff; no refetch | [DOT-148](http://localhost:3100/DOT/issues/DOT-148) → [DOT-149](http://localhost:3100/DOT/issues/DOT-149) | `7e857107…` → `95aedf07…` | Child summary appears in provider prompt; parent invokes only semantic finish and settles once | Both issues done in 24s total. Persisted native input contains `reason: issue_children_completed` and the child summary. Parent made no discovery/history/search/document/shell/file calls; browser shows one 8-second activity and one final reply | Captured | PASS |
| 14:56Z | GLM Flash child → Codex app-server parent | Cross-provider child return | [DOT-150](http://localhost:3100/DOT/issues/DOT-150) → [DOT-151](http://localhost:3100/DOT/issues/DOT-151) | captured | Exact child summary reaches a Codex parent without refetch or duplicate work | Parent emitted immediate reasoning/commentary, invoked only semantic finish, rendered one final reply, and both issues settled done | Captured | PASS |
| 14:58Z | GLM Flash child → ACPX Claude parent | Cross-provider child return through ACPX | [DOT-152](http://localhost:3100/DOT/issues/DOT-152) → [DOT-153](http://localhost:3100/DOT/issues/DOT-153) | captured | Protocol handoff succeeds; cheap child quality remains distinguishable from transport correctness | Parent received the exact wake and did not refetch. Child supplied an incorrect string reversal, correctly classified as model-quality/self-claim risk rather than a protocol failure | Captured | PROTOCOL PASS / MODEL QUALITY FAIL |
| 15:03Z | Codex app-server / `gpt-5.6-luna` | Governed suggested-task selection (pre-fix control) | [DOT-154](http://localhost:3100/DOT/issues/DOT-154) → [DOT-155](http://localhost:3100/DOT/issues/DOT-155) | `623cffe2…` → `cf44af4e…` | Create only the selected child, then resume the native session once | Selection UI created only DOT-155, but native input rejected `suggest_tasks`; a 75ms legacy failure then triggered an unnecessary review-path recovery | Captured | FAIL / FIXED |
| 15:08Z | Codex app-server / `gpt-5.6-luna` | Governed suggested-task selection (post-fix) | [DOT-156](http://localhost:3100/DOT/issues/DOT-156) → [DOT-157](http://localhost:3100/DOT/issues/DOT-157) | `f9d9926f…` → `8332344f…` | One selected child; one native response wake; commentary/tool status; authoritative done | Browser selected only calculator; exactly one unassigned DOT-157 was created. The original Codex session resumed natively, streamed commentary and search, reported the exact selection, and committed done. Exactly two total wakes (assignment + interaction), no recovery | Captured | PASS |
| 15:10Z | Codex app-server / `gpt-5.6-luna` | Native checkbox selection | [DOT-158](http://localhost:3100/DOT/issues/DOT-158) | captured | Tests-only selection resumes natively and completes once | Polished checkbox card accepted exactly `tests`; same-session continuation reported that exact ID and committed done | Captured | PASS |
| 15:14Z | Codex app-server / `gpt-5.6-luna` | Incremental item verdicts (clean post-fix replay) | [DOT-160](http://localhost:3100/DOT/issues/DOT-160) | `a43c1a52…` → `6adac669…` → final | Partial `alpha=approve` parks on the same interaction; final `beta=reject` completes | Partial wake settled as a governed wait in 942ms without replacement interaction or provider retry. Final wake reported both exact verdicts and committed done | Captured | PASS |
| 15:16Z | Codex app-server / `gpt-5.6-luna` | Yielded-wait presentation | [DOT-161](http://localhost:3100/DOT/issues/DOT-161) | `04751fab…` → final | Interaction card and compact wait state, no artificial assistant comment; final response after selection | Pending state had zero issue comments and presentation reason `yielded_control_plane_wait`; browser showed the card plus activity. Selecting blue produced one exact final `blue` reply and done | Captured | PASS |
| 15:27Z | OpenCode / OpenRouter GLM | Ask-mode canonical human input after disabling provider-native questions | [DOT-166](http://localhost:3100/DOT/issues/DOT-166) | `b32ae336…` → `a8f34a8c…` | Expose only Paperclip's durable question path; answer detailed; exact two-sentence final; done | OpenCode invoked `paperclip_request_human_input`, created one required single-select card, parked without a synthetic comment, consumed `detailed` in one continuation, rendered exactly two sentences, and committed done | Captured | PASS |
| 15:29Z | OpenCode / OpenRouter GLM | Governed-wait cancellation presentation | [DOT-167](http://localhost:3100/DOT/issues/DOT-167) | `f7c392f4…` → `ff21022e…` | A normal OpenCode `/abort` used to park the turn must not look like a provider failure | Initial run emitted `turn.cancelled` plus accepted yielded result, no error notice or failed turn. Browser showed only `Ran a tool` and the live question card; `energetic` resumed once and produced one exact sentence, issue done | Captured | PASS |
| 15:33Z | ACPX Claude / `claude-sonnet-5` | Canonical ask interaction plus final response (pre-fix control) | [DOT-168](http://localhost:3100/DOT/issues/DOT-168) | `1eb86101…` → `aa3e51c4…` | Ask, park, resume with bullets, show the actual bullet response | Interaction and continuation were correct, and raw PRP deltas contained all three DNS bullets, but ACPX never completed a final `agentMessage`; Paperclip therefore materialized the semantic status summary instead | Captured | FAIL / FIXED |
| 15:35Z | ACPX Claude / `claude-sonnet-5` | ACPX final-channel boundary (post-fix) | [DOT-169](http://localhost:3100/DOT/issues/DOT-169) | `460ad279…` | Preserve provider prose at semantic completion; exclude trailing acknowledgement; final-agent precedence | Presentation chose `final_agent_message`, materialized only the provider's three bullets, and kept the later acknowledgement inside collapsed activity. Browser shows one primary bullet response and issue done | Captured | PASS |
| 15:39Z | ACPX Codex / `gpt-5.6-sol` | Final-channel boundary plus semantic completion (pre-fix controls) | [DOT-170](http://localhost:3100/DOT/issues/DOT-170) → [DOT-175](http://localhost:3100/DOT/issues/DOT-175) | multiple bounded controls | Preserve final text and allow only runner-owned completion tools | DOT-170 proved the completed final-message boundary, but Codex-ACP routed its MCP approval through an advertised form-elicitation path that the headless runner did not own. Every control failed closed with `user cancelled MCP tool call`; retries were bounded and controls were cancelled before the next variant | Captured metadata | EXPECTED CONTROLS |
| 15:55Z | ACPX Codex / `gpt-5.6-sol` | Governed permission-channel semantic completion | [DOT-176](http://localhost:3100/DOT/issues/DOT-176) | `6942a12b…` | One approved Paperclip completion call; no external MCP auto-approval; accepted result; done | Removing the false generic-form capability forced Codex-ACP through the correlated permission channel. `paperclip_finish` returned accepted, one run succeeded in 17s, issue committed done, and no recovery turn followed. The model omitted the requested literal two-line prose, so the designed semantic-summary fallback became the primary reply | Captured | PROTOCOL PASS / MODEL COMPLIANCE MISS |
| 15:58Z | ACPX Codex / `gpt-5.6-sol` | Explicit pre-tool final response plus schema self-correction | [DOT-177](http://localhost:3100/DOT/issues/DOT-177) | `2f5de1d6…` | Stream exact final first; surface invalid completion calls; self-correct; deduplicate settled reply; done | Exact `LINE ONE\nLINE TWO` streamed before tools. Four malformed completion attempts received specific schema errors; the fifth was accepted without a new provider turn. Browser shows collapsed `Worked for 53s` and one primary exact reply, issue done | Captured | PASS |
| 16:03Z | ACPX Codex / `gpt-5.6-sol` | Raw ACP debugger controls and topology validation | [DOT-178](http://localhost:3100/DOT/issues/DOT-178) → [DOT-180](http://localhost:3100/DOT/issues/DOT-180) | bounded controls | Capture ACP traffic at the native object boundary and classify completeness by provider topology | First clean reload captured 32 frames, proving the adapter hook; the trace store then correctly exposed its old Codex-specific `rust_native` completeness assumption. DOT-180 captured 28 frames after the native callback survived runtime-owner rebinding | Sidecar controls | FIXED |
| 16:09Z | ACPX Codex / `gpt-5.6-sol` | Complete raw ACP trace | [DOT-181](http://localhost:3100/DOT/issues/DOT-181) | `4b87f7a2…` | Complete status, ordered bidirectional ACP frames, inspector usability | 28 frames / 32,765 bytes, 4 client→provider and 24 provider→client, digest verified. Native methods include `initialize`, `session/new`, `session/prompt`, `session/request_permission`, and `session/update`; inspector rendered all frames and stages | Complete | PASS |
| 16:12Z | ACPX Codex / `gpt-5.6-sol` | Raw ACP frame → PRP lineage | [DOT-182](http://localhost:3100/DOT/issues/DOT-182) | `bbbabdd2…` | Correlate provider frames to canonical PRP and visible/folded presentation | 28 complete frames; 10 runtime-normalization stages link 10 provider frames to 10 PRP event IDs. Browser inspector selected frame 8 and showed raw JSON, transport + normalization stages, `item.completed`, and its visible/folded production decision | Complete | PASS |
| 16:15Z | ACPX Claude / `claude-sonnet-5` | Cross-provider ACP trace, immediate commentary, and exact final | [DOT-183](http://localhost:3100/DOT/issues/DOT-183) | `66f0c428…` | Prove the provider-neutral ACP hook on Claude, including commentary order, exact durable final, redaction, and semantic completion | One early commentary comment precedes one exact final. Trace is complete with 49 frames / 52,351 bytes, 7 client→provider and 42 provider→client, 32 correlated runtime events, and three redacted frames. Browser shows collapsed activity plus one primary final | Complete | PASS |
| 16:17Z | ACPX Claude / `claude-sonnet-5` | Semantic tool frame → one-to-many PRP lineage | [DOT-184](http://localhost:3100/DOT/issues/DOT-184) | `eb90adf3…` | Link the originating ACP approval/tool frame to tool start, semantic result, and tool completion | Inspector groups frames 27–28 with exactly three PRP events: `item.started`, `run.result.proposed`, and `item.completed`. Trace is complete with 38 frames. Claude omitted the requested literal final, so the accepted semantic summary was used correctly | Complete | PROTOCOL PASS / MODEL COMPLIANCE MISS |

## Defect ledger

| ID | Evidence | Layer | Fix | Regression | Live retest |
| --- | --- | --- | --- | --- | --- |
| STRESS-001 | DOT-102 / DOT-104 rejected `ask` although PRP actions and UI support ask mode | Runtime admission + native execution contract | Admit `ask` for `paperclip_runner`, round-trip it in v2/v3 model envelopes, retain the old Codex-local boundary, and expose ask-safe semantic tools | Runner contract 2 tests; server runtime mode 1 test; focused suites 50/50 pass; runner/server typechecks pass | DOT-106 admitted; terminal result pending |
| STRESS-002 | DOT-103: `ACPX native backend requires its own instance runtime directory` | runnerd-to-provider backend wiring | Pass the isolated instance ACPX directory at the native backend factory seam | Server backend wiring test; focused server suite 35/35 pass | DOT-108 launched and executed its file command; terminal result pending |
| STRESS-003 | DOT-107 live ACPX Claude turn: provider notice `Authentication required` after static probe passed | Environment qualification / provider auth | Bound the existing encrypted company Anthropic secret to the ACPX Claude agent; no plaintext configuration or logging | Live environment qualification passed | DOT-110 authenticated, streamed, emitted a semantic result, and completed `done` |
| STRESS-004 | DOT-108 implemented and verified the file, then its semantic-finalization retry treated a transient run workspace binding as a missing persisted workspace | Native recovery workspace resolution | Distinguish transient run-id workspace bindings from persisted `execution_workspaces`; reuse only an existing persisted row while retaining fail-closed behavior for archived rows | Workspace-session + runtime mode + executor suites 180/180 pass; server typecheck passes | Fresh projectless retries in DOT-113 resumed the same provider session instead of failing setup |
| STRESS-005 | DOT-109 ACPX Pi runtime approval requests were cancelled by the provider before an immediate administrator resolution reached the live session | Runtime request lifetime / response routing | Investigation pending; preserve cancelled request and failed-tool lineage and ensure no run hang | Live request/cancellation records | DOT-109 still progressing after cancellation |
| STRESS-006 | DOT-114 rendered only “I’ll create the file…” although a later accepted semantic summary existed | OpenCode text-channel normalization / presentation selection | Record text completion source sequence and require a settled final candidate to occur after the last non-terminal work tool; when none exists, emit no final `agentMessage` so the semantic summary wins | OpenCode fixture `commentary-only-before-work`; focused protocol/driver suites pass | DOT-116 shows the exact post-work response and verification caveat as the durable final reply |
| STRESS-007 | DOT-113 exact frame 3322: `paperclip_paperclip_finish`, `completed`, criterion `passed` rejected by the canonical schema | Provider-neutral semantic result normalization | Normalize known completion schema, disposition, and criterion-status aliases before canonical validation; retain strict rejection for unknown values | Exact GPT-OSS-shaped regression in `result-normalization.test.ts`; focused suites pass | DOT-113 recovered and completed `done` with canonical accepted result |
| STRESS-008 | DOT-113 recovery checkpoint contained the same turn in both `activeTurnId` and `terminalTurns`; the resumed runtime waited forever for an already-consumed idle event | Driver checkpoint normalization + native recovery orchestration | All drivers clear a terminal turn from active state before emitting/persisting terminal facts; recovered driver state is authoritative and the runtime no longer resurrects the older persisted active ID | OpenCode driver checkpoint regression plus native runtime continuation regression; 74/74 focused tests and complete runner typecheck pass | DOT-113 resumed and completed on the original provider session |
| STRESS-009 | DOT-113 aliases were accepted by post-dispatch normalization but rejected earlier by the provider-facing MCP bridge | Shared completion contract / MCP admission | Make the provider schema accept only the documented compatibility aliases, then normalize into the strict canonical output schema; add verification aliases `pass/success/succeeded/fail` | Completion-contract, normalization, MCP bridge, and driver focused suites 35/35 pass | DOT-113 accepted live after rebuild |
| STRESS-010 | DOT-117 accumulated watchdog `attemptCount: 9` across different run fingerprints and terminal exhaustion left the failed low-capability model as its own inactive recovery owner | Native session recovery and issue disposition | Supersede watchdogs when run/failure identity changes; on attempt-3 result-less exhaustion preserve named failure authority, move issue to `in_review`, and create a board-owned recovery action with the source agent as return owner | Native recovery projection regression; native session/resumption/recovery suites 78/78 pass; server and runner typechecks pass | DOT-121 is `in_review`, board-owned, attempt 1/3, named `native_session_retry_exhausted`, with no extra provider run |
| STRESS-011 | DOT-123 raw OpenCode frame 128 contained `callID=functions.todowrite:0`, `state.error=Tool execution aborted`, while PRP/UI showed `Tool failed · unknown` with empty output | OpenCode provider-event normalization | Recover a display label only from structured `functions.<name>:<index>` call IDs when the provider reports `unknown`; preserve bounded terminal `state.error` as output; opaque call IDs remain opaque | Provider-event regressions for structured and opaque call IDs; OpenCode/provider suites 25/25 pass | Rebuild and live tool-failure retest pending |
| STRESS-012 | DOT-123 OpenCode replayed accepted finish as `tool=unknown`, `callID=functions.paperclip_paperclip_finish:24`; the driver treated it as later work and selected a short trailing acknowledgement instead of the substantive response | OpenCode final-response boundary | Canonicalize structured call-ID tool identity before deciding whether a part is terminal semantic work; terminal replays no longer move the work boundary | Fake OpenCode now injects the exact unknown terminal replay before a trailing acknowledgement; substantive final selection suite passes | Fresh Kimi/GLM live retest pending |
| STRESS-013 | DOT-125 emitted a substantive final before same-session semantic-result recovery, then a terse acknowledgement after recovery; latest-message precedence displaced the actual answer | Response resolution + native recovery envelope | Mark semantic-result recovery structurally, select the best completed final before that boundary, and send a disposition-only recovery prompt that forbids redoing implementation or adding prose | Heartbeat summary and native-session-runtime regressions; full runner TS 684/684 and Rust 92/92 pass | DOT-126 normal path passes; deterministic live recovery replay still pending |
| STRESS-014 | Historical DOT-119 recovery actions exceeded their configured maximum (`attemptCount` reached 23) and continued scheduling wakes | Central recovery action policy | Enforce the effective max in the shared service; atomically escalate to board ownership, clear wake/monitor policies, cap attempts, preserve return owner, and make replays no-ops while allowing distinct failure identities to supersede | Recovery-action regression plus native executor/process recovery suites 187/187 pass | Historical row now projects exhausted; fresh live exhaustion already bounded at runtime in DOT-121 |
| STRESS-015 | DOT-127 stored a steering comment at submission time and rendered it above the entire settled run, despite a later durable steering acknowledgement | Issue conversation projection | Derive the causal slot from the first non-duplicate `issue.queued_comment_steered` event and split Paperclip settled activity at each acknowledgement so both collapsed and expanded timelines preserve pre-steer → human → post-steer → final order | TaskChatThread causal regression; 24/24 component tests, UI typecheck and production build pass | Browser verified positions: Worked 757, steering 957, post-steer Worked 1161/commentary 1194, final 3498 |
| STRESS-016 | DOT-130 review card did not visibly identify its human owner | Review presentation | Render durable addressee and resolver policy on the review card | UI component suite and build pass | DOT-130 shows `Assigned to You · board only` |
| STRESS-017 | DOT-132 cancelled turns left unfinished tool/provider rows visually Running | Terminal presentation normalization | Project unfinished provider/tool activity as Interrupted whenever the authoritative terminal is cancelled/interrupted | Transcript and task-thread regressions pass | DOT-132 browser shows `Stopped` with no stuck-running terminal activity |
| STRESS-018 | Native semantic-result retries depended on the periodic scheduler and could miss their exact 30-second deadline | Same-run recovery timer | Arm an in-process timer for the persisted retry deadline while retaining durable restart recovery | Native runtime timing regressions pass | Live retries fire at the intended 30-second boundary |
| STRESS-019 | ACPX response-wake checkpoints lost `providerIdentity` in the harness/native backend conversion | Backend checkpoint identity | Round-trip the provider identity through `HarnessDriverBackend` | Focused backend tests and full runner typecheck pass | ACPX continuation reached the provider instead of failing the identity assertion |
| STRESS-020 | Generic stranded-issue recovery could launch another run after native retry exhaustion had already created a board-owned action | Cross-run recovery ownership | Skip generic recovery when an active board-owned recovery action already owns the issue | Process-recovery regression passes | DOT-131 stayed at attempt 3 with no fourth provider run |
| STRESS-021 | Cheap ACPX models continued polling after creating a durable human question | Governed-wait runtime control | Detect the committed wait after each event, cancel the provider turn, and synthesize authoritative yielded terminal facts | Native runtime governed-wait regression passes | DOT-134–136 parked in 9–22s with only legitimate pre-wait tools |
| STRESS-022 | ACPX Pi's persisted child session cannot resume after a governed wait, even with intact identity | ACPX response-wake recovery policy | Permit a fresh provider record only after a yielded `response_wake`, while preserving normalized Paperclip session lineage and strict policy elsewhere | ACPX host/driver and resume tests pass | DOT-136 consumed the answered Maple choice on the replacement provider record |
| STRESS-023 | Browser runtime approval routing existed only for runnerd transports; in-process ACPX/OpenCode sessions had no control-plane authority registration | Native runtime request routing | Add direct active-session resolution with turn binding, idempotency, conflict detection, and runnerd fallback | Backend and server typechecks/tests pass | DOT-138 API and browser UI resolutions both returned accepted and resumed Pi |
| STRESS-024 | Pi treated every slash inside a relative shell path as an absolute path, rejected creation when intermediate directories did not exist, and did not persist `accept_for_session` | ACPX Pi workspace/permission extension | Match absolute paths only at shell-token boundaries, reject parent traversal segments, canonicalize physical roots, walk to the nearest existing ancestor safely, and cache session approval for the extension lifetime | Pi extension regression, runner typecheck, Rust check, and full runner build pass | DOT-139 created a nested file after exactly one approval and completed with exact-byte evidence |
| STRESS-025 | DOT-140's native completion review existed durably but the open issue page did not refresh | Realtime UI invalidation | Invalidate the active issue conversation when native status-decision effects materialize an interaction | Live-updates regression; 35 UI tests, typecheck, and production build pass | DOT-140 rendered the review card without reload and approval completed |
| STRESS-026 | Rejecting DOT-142's `wake_assignee_on_accept` plan interaction did not wake the planner for revision | Interaction continuation policy | Treat rejection of the current issue plan as a revision wake even when acceptance has an accept-only continuation policy | Issue-thread route suite 73/73 pass | DOT-142 revision 2 woke immediately, used the exact rejection note, and did no implementation |
| STRESS-027 | Child completion enqueued both generic dependency and parent wakes; dispatcher coalescing let the poorer reason win, and the comment race produced a null summary | Native status effect materialization | Coalesce the parent/dependency edge at the committer; include child lineage once; source the just-finished child's summary from the accepted native result before presentation comments exist | Native control-plane regression; 10/10 pass | DOT-146 wake reason is `issue_children_completed` and carries the full DOT-147 accepted summary |
| STRESS-028 | DOT-146's run snapshot contained the child summary, but the closed native execution input sent only the task brief to the provider; GLM rediscovered history and files | Native execution-input projection | Render the sanitized provider-neutral Paperclip wake delta into every native task prompt, using resume-delta form for a continued provider session | Native session/input and control-plane suites 17/17 pass; server typecheck passes | DOT-148 persisted the child handoff in its provider prompt and completed in 8s with no discovery tools |
| STRESS-029 | DOT-142 was done from a newer child-return run, then an older accepted-plan finalizer exhausted and reopened it to board review | Native finalization ordering | Detect a newer run owning the issue's latest authoritative decision; retire the older coordinator as `native_finalization_superseded`, resolve its recovery as a false positive, and never mutate issue state | Native finalization recovery integration 2/2 pass | DOT-142 recovery was resolved and the issue restored to the later authoritative `done` decision |
| STRESS-030 | DOT-142 evidence `interaction:<uuid> (request_confirmation, status accepted)` was passed wholesale to a Postgres UUID comparison and crashed finalization | Durable evidence classification | Extract a canonical UUID prefix from annotated model evidence; malformed values remain unverifiable rather than reaching a UUID query | Evidence-classifier plus native finalization/input/control-plane suites; 23/23 pass | Original failure identified exactly from coordinator evidence; fresh annotated-reference integration fixture passes |
| STRESS-031 | DOT-154 accepted `suggest_tasks` through the polished board UI, then native response materialization rejected the interaction kind, fell back to a 75ms legacy failure, and launched `issue_review_path_lost` recovery | Native interaction-response contract + continuation routing | Extend the provider-neutral native response envelope and authorized bridge to all five PRP interaction kinds, including partial item verdicts; keep governed tool approvals excluded; do not attach fallback review-loss instructions to a normal accepted-response wake | Native contract 15/15; bridge 5/5; server typecheck passes | DOT-156 resumed the original Codex session through exactly one native interaction wake, created only DOT-157, streamed normally, and committed done without recovery |
| STRESS-032 | DOT-159 delivered an authoritative partial item verdict, but Codex had to recreate a wait, the duplicate request failed, and semantic-result recovery began | Native governed-wait ownership | Treat an authority-checked pending `request_item_verdicts` response as the continuation run's existing wait target; park immediately after the next committed event or completed turn | Native executor + interaction bridge suites 31/31 pass | DOT-160 partial continuation settled in 942ms on the same interaction, then the completed response resumed and finished done |
| STRESS-033 | DOT-160 materialized identical `Waiting for Proposal verdicts` assistant comments from top-level adapter summaries even though yielded semantic results are control-plane liveness facts | Response resolver / settled presentation | Suppress legacy top-level summaries whenever the accepted semantic result is `yielded`; preserve explicit comments, final agent messages, and marked finals; show the durable interaction/system wait instead | Response-resolver, executor, and bridge suites 58/58 pass; server typecheck passes | DOT-161 pending state has zero issue comments and `commentAction:none`; browser shows the interaction card/activity, then one real final after selection |
| STRESS-034 | OpenCode's provider-native `question` tool created an invisible provider-side wait, while ask-mode also omitted Paperclip's canonical `request_human_input` from the dynamic tool catalog | Provider tool policy + semantic action modes | Disable OpenCode `question` in both runtime config and every prompt; add `ask` to the canonical, live, and scenario task modes for `request_human_input` so every provider receives one durable governance path | OpenCode driver 14/14, catalog 8/8, runner authority 7/7 pass; runner rebuilt | DOT-166 exposed only the canonical Paperclip operation, created one card, consumed one answer, and completed without a provider-native wait |
| STRESS-035 | A healthy OpenCode governed wait displayed `Received a provider notice` because OpenCode represents normal `/abort` control as `session.error` / `MessageAbortedError` | Provider terminal normalization | Normalize that exact OpenCode abort shape to `turn.cancelled` without a false error notice; retain all other `session.error` events as failures | Exact fake-provider abort regression; OpenCode driver 14/14 passes | DOT-167 has `turn.cancelled`, accepted yielded result, no provider notice/failed turn, and browser shows only the question activity before successful continuation |
| STRESS-036 | ACPX streamed the genuine user-facing answer only as `item.delta`; with no completed final `agentMessage`, the response resolver showed the model-authored status summary instead | ACPX final-answer normalization | Track assistant prose after the last non-terminal tool, snapshot it when `paperclip_finish`/`paperclip_block` is accepted, and emit one canonical completed final message on successful settlement; exclude post-result acknowledgements and preserve result-less finals | ACPX final-boundary + runtime-host suite 13/13 passes; runner typecheck/build pass | DOT-169 chose `final_agent_message` and rendered only the upstream bullets; the later `Completed` acknowledgement remained folded in activity |
| STRESS-037 | ACPX Codex cancelled every Paperclip semantic MCP call even though the same runner-owned operations worked through Claude and Pi | ACPX capability advertisement + pinned Codex-ACP approval correlation | Do not advertise generic ACP form elicitations to Codex because Paperclip owns durable user interaction through PRP; patch the pinned Codex-ACP permission request to retain `serverName`; auto-approve only the closed set of runner-created MCP servers, retaining interactive policy for filesystem/process and future external tools | Codex-ACP patch dry-run valid; lockfile patch hash refreshed; ACPX driver/host 14/14, runner typecheck and full build pass | DOT-176 accepted `paperclip_finish`, emitted a native semantic result, committed `done`, and launched no recovery retry |
| STRESS-038 | ACPX agents had tracing enabled but produced zero provider frames; once captured, the store incorrectly demanded Codex's Rust + rehydration channels and marked the TypeScript-native ACP trace incomplete | ACPX transport trace + provider-neutral trace integrity | Forward ACP JSON-RPC objects from the pinned ACPX runtime into the restricted trace sink, preserve direction/method/digest, retain the callback across runtime owner rebinding, require `typescript_acpx_native` for ACPX completeness, and link runtime-normalized PRP event IDs back to inbound frames | ACPX patch dry-run valid; patch hash refreshed; host/driver 14/14; trace-store 5/5; runner build/typecheck and server typecheck pass | DOT-181 is complete with 28 frames; DOT-182's inspector shows two stages and a real PRP/presentation outcome for correlated frames |
| STRESS-039 | In DOT-183 the provider transport and ordinary runtime events were correlated, but the model-authored `paperclip_finish` PRP rows remained detached as “no raw correlation” | ACPX semantic-tool bridge lineage | Capture the current inbound ACP frame when the dynamic tool dispatch begins and attach all emitted PRP IDs, including validation failures and tool results, as one mapped normalization record | ACPX host regression asserts a three-event one-to-many mapping; ACPX host/driver 14/14 and full runner typecheck/build pass | DOT-184 groups the `paperclip_finish` frame pair with `item.started`, `run.result.proposed`, and `item.completed`; no detached semantic-result row remains |
| STRESS-040 | The superseded pre-fix DOT-154 control retained an old agent-owned recovery action and woke again during the final liveness audit | Campaign control cleanup / recovery ownership | Cancel the obsolete control run and retire the pre-fix issue; keep DOT-156 as the authoritative post-fix evidence | Final live-run query returns zero; every remaining nonterminal campaign issue has an active named recovery path or pending board interaction | DOT-154 is cancelled and its recovery action is cleared; no background run remains |
| STRESS-041 | The final server sweep showed that `chosenSource:none` also disabled the one bounded disposition/comment recovery for a genuine legacy adapter with no semantic result | Legacy completion recovery / response presentation boundary | Suppress artificial-comment recovery only when an accepted semantic result exists or a legacy adapter summary was deliberately classified ambiguous; preserve the disposition-only retry for truly result-less legacy runs. Refresh the stale accepted-plan prompt assertion to the current single-child directive | `heartbeat-comment-wake-batching` + `heartbeat-accepted-plan-workspace-refresh`: 17/17 pass; response resolver: 27/27 pass; server typecheck pass | Deterministic gateway fixture now queues exactly one bounded recovery, while native no-response/yielded results remain suppressed |
| STRESS-042 | The broad route-inventory gate found the provider-trace and queued-steering endpoints mounted but absent from the public OpenAPI document | API discoverability / contract inventory | Document provider-trace list, redacted inspect, exact reveal, NDJSON download, deletion, queued-comment operations, and the two existing agent-profile route groups; register their route files in the inventory | OpenAPI route suite 5/5 pass; server typecheck pass | All mounted routes are represented and the debugger API is discoverable in the local OpenAPI document |
| STRESS-043 | Server startup tests predated the Runner PRP WebSocket upgrade listener and supplied an HTTP server mock without `.on()` | Startup integration test contract | Extend the test server double with the standard event-listener method used by the Runner WebSocket boundary | Server startup suite 16/16 pass | Startup wiring, feedback export, scheduler suppression, and public URL cases all remain green with the Runner listener installed |
| STRESS-044 | Project skill discovery canonicalized macOS `/var` paths to `/private/var`, then rejected them against the persisted noncanonical workspace root; case-insensitive filesystems also classified lowercase `skill.md` as canonical | Project skill portability / path safety | Validate containment on canonical pairs while retaining lexical symlink traversal when possible; traverse the verified canonical pair only for equivalent path aliases; require an exact `SKILL.md` directory entry; make path assertions realpath-aware | Full company-skills service suite 56/56 pass; server typecheck pass | Valid project skills import on macOS, out-of-workspace and symlink candidates still fail closed, and lowercase lookalikes are not advertised as skills |

## Coverage checklist

- [x] Immediate commentary and ongoing status (OpenCode Ultra; ACPX Codex streamed pre-tool text)
- [x] Streaming assistant output; empty and non-empty reasoning (OpenCode Ultra; ACPX Codex thinking marker)
- [x] Native tool calls, search, file and command activity (DOT-128, DOT-129, DOT-139)
- [x] Pure Q&A and long final response (DOT-110)
- [x] Exact file creation and exact-byte verification (DOT-139 exact 9-byte file and semantic completion)
- [x] Small code/test task and multi-file implementation (DOT-105)
- [x] Failed command and unavailable verifier result (DOT-129 recovered failed tool; DOT-116 accepted and displayed `not_run/tool_unavailable` caveat)
- [x] Structured user question and exact continuation (DOT-122, browser and exact-byte evidence)
- [x] Governed suggested-task selection and same-session continuation (DOT-156 → DOT-157)
- [x] Checkbox selection and incremental item-verdict continuation (DOT-158, DOT-160)
- [x] Planning result and owned confirmation handoff; rejection/revision, acceptance, delegated implementation, and parent return (DOT-142 → DOT-143)
- [x] Queued follow-up ordering and narrow continuation (DOT-123); true in-turn steering and idempotent replay (DOT-127)
- [x] No-response terminal state without synthetic reply (DOT-132: zero comments, compact `Stopped`; resolver/system-notice regressions)
- [x] Subtask creation, parent/child dependency blocking, delegated implementation, child completion, summary handoff, and parent return (DOT-142–149)
- [x] Runtime permission approval and session-scoped continuation (DOT-138/139); human completion-review rejection, narrow continuation, and approval (DOT-130)
- [x] Forced server restart/process loss, bounded retry, session reuse, and successful recovered completion (DOT-115); cancellation/interruption presentation (DOT-132)
- [x] Duplicate steering delivery and idempotency (DOT-127); general PRP duplicate and terminal-authority fixtures (113-test edge corpus)
- [x] Trace capture on/off and raw-to-presentation lineage (DOT-122 inspector; DOT-124 capture-off)
- [x] Unknown/ignored/rejected/generic/one-to-many/many-to-one mappings (provider/result/generic-capability fixtures; Codex result call 1→2; DOT-128 correlated 4 frames→6 PRP events)
- [x] Terminal status, disposition, response resolution, collapsed activity, steering-boundary split, and no-response system notice (DOT-116, DOT-121, DOT-125–127, DOT-132)

## Automated conformance ledger

| Suite | Scope | Result |
| --- | --- | --- |
| `native-execution.test.ts` | v1/v2/v3 contract, including ask-mode round-trip and plan rejection | 15/15 pass |
| `runtime-mode.test.ts` + `native-session-executor.test.ts` | admission, recovery/steering/session seams, ACPX runnerd directory | 35/35 pass |
| `@paperclipai/paperclip-runner typecheck` | TypeScript, Rust workspace, browser bundle | PASS |
| Full Runner TypeScript suite | All driver, contract, catalog, eval, replay, trace, and presentation tests; generated operation-group and seeded-catalog drift gates included | 691/691 pass after refreshing both canonical generated artifacts |
| Full Runner Rust workspace | Provider transports, durable state/outbox, exact trace bytes, replay, steering, process cleanup, and contract integration | 100/100 pass |
| Protocol inventory and cross-language parity | 41-operation coverage/inventory plus TypeScript↔Rust conformance and 16 replay fixtures | PASS |
| `@paperclipai/server typecheck` | Server integration and vendored runner boundary | PASS |
| OpenCode + Codex driver, native recovery, result normalization focused suites | Text selection, alias compatibility, stale terminal checkpoint recovery | 74/74 pass |
| Provider/trace edge corpus | Exact Rust/TypeScript correlation, unknown provider methods, generic fallback safety, ignored/rejected result records, and one-to-many result lineage | 113/113 pass |
| Response resolver | Explicit/final/semantic precedence, yielded waits, and no-response/no-placeholder behavior | 27/27 pass |
| Runner Inspector + system notices | Correlated grouping/pipeline plus compact no-response terminal presentation | 23/23 pass |
| Final server + UI sweep | Trace storage/finalization/interaction plus inspector/thread/presentation regressions | 22/22 server and 114/114 UI pass |
| Final legacy-recovery isolation | Result-less gateway retry, mentioned-agent deferral, accepted-plan continuation, and response resolution after STRESS-041 | 44/44 pass; server typecheck PASS |
| Final repaired server aggregate | Legacy recovery, accepted-plan continuation, response resolution, OpenAPI parity, startup wiring, and project-skill portability | 121/121 pass across 6 files; server typecheck PASS |
| Broad server stress sweep | Whole-suite concurrency plus isolated reruns outside the Runner scope | Runner-related failures, OpenAPI drift, startup mock drift, and project-skill path portability isolated and fixed. Remaining failures reproduce as repository test-harness drift: macOS workspace-path assertions, synthetic port exhaustion, and unrelated workspace registration fixtures |

## Known gaps and remaining risks

- ACPX Pi is runnable and DOT-139 proved session approval plus an exact nested write;
  it remains more likely than stronger models to exhaust the strict completion schema.
- Very small/cheap models can discover and call `paperclip_finish` yet repeatedly
  fail its strict schema. Bounded recovery works, but schema ergonomics and error
  visibility need investigation before this lane can be considered reliable.
- Historical local issues include deliberately broken and legacy runs. Campaign
  conclusions use newly created issues plus isolated fixtures, not old status alone.
- The final ownership audit found no live runs. Remaining nonterminal stress controls
  have an active board/agent recovery action or a pending board interaction; the one
  superseded control that woke unexpectedly (DOT-154) was cancelled and retired.
- The full server suite is not globally green in this checkout. Isolated failures are
  outside the Runner protocol path and are recorded above: macOS realpath-sensitive
  workspace assertions, synthetic test-port exhaustion after a long campaign,
  and workspace source-registration fixtures. Dedicated Runner, protocol,
  response, trace, completion, server-integration, and UI suites are green.
